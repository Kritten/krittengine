import { mat4, vec3 } from 'gl-matrix';

export const VECTOR_UP = vec3.fromValues(0, 1, 0);

export const DIRECTION_RAY_INITIAL = vec3.fromValues(0, 0, -1);

export const DUMMY_MAT4 = mat4.create();

export const DUMMY_VEC3 = vec3.create();
